/**
 *  5.37 Node, ES6 and Template Strings
 *
 *  Template literal: a way to concatenate strings in JavaScript (ES6). Easier to work with than a bunch of strings
 *  concatenated with '+'.
 */
var name = 'Mau Mau';
var name2 = `\nHello ${name}`; // a backtick says this will be a template literal

console.log(name2);